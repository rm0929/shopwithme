# Scripts

| Script | Description |
|--------|-------------|
| `start-catalog.sh` | Start catalog + PostgreSQL via Docker Compose |
| `stop-catalog.sh` | Stop Docker Compose services |
| `run-catalog-local.sh` | Run catalog locally with JSON storage (no Docker) |

Run from project root:
```bash
./scripts/start-catalog.sh
```
