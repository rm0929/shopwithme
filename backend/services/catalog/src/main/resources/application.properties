spring.application.name=catalog
server.port=8081

# Storage: 'json' (file-based) or 'postgres' (PostgreSQL database)
spring.profiles.active=${CATALOG_STORAGE:json}

# OpenAPI / Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
